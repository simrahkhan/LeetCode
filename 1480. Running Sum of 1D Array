class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        outp = []
        adder = 0
        for i in nums:
            adder += i
            outp.append(adder)
        return outp
